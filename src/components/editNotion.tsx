import React from 'react';

export const EditNotion = React.memo(() => {
        return (
            <div className={'counterBody'}>
                <div className="counterBody_box">
                <span
                    className={'countValue Limited'}>Edit mode</span>
                </div>
            </div>
        );
    }
);
